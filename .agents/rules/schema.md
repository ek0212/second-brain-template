---
paths:
  - "schema/**/*.md"
  - "AGENTS.md"
  - ".agents/**/*.md"
---

# Schema And Instruction Rules

- Keep `AGENTS.md` short, stable, and broadly applicable.
- Put path-sensitive constraints in `.agents/rules/`.
- Put multi-step workflows in `.agents/skills/`.
- Do not duplicate the same rule across many files.
- If the vault structure stops fitting real notes, update the schema before reorganizing broadly.

